# Longest Consecutive 1's After One Swap

## Problem Description

Given a binary string **A**, you are allowed to perform **at most one swap** between any `0` and `1`.

Find and return the **maximum length of consecutive 1’s** that can be obtained after at most one swap.

## Constraints

- `1 <= length of string <= 1,000,000`
- String `A` contains only characters `'0'` and `'1'`

