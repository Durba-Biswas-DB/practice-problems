# Min XOR Value

## Problem Description

Given an integer array `A` of `N` integers, find the **pair of integers** in the array which have the **minimum XOR value**.

You need to **return the minimum XOR value** among all possible pairs.

---

## Constraints

- `2 ≤ N ≤ 100000`
- `0 ≤ A[i] ≤ 10^9`
